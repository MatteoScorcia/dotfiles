# My configs :)

## htop

### Brew requirements

`brew install htop`

## nvim

### Brew requirements

`brew install nvim`
`brew install fzf`
`brew install ripgrep`
`brew install asdf`
`asdf install nodejs lts`
`asdf global nodejs lts`
